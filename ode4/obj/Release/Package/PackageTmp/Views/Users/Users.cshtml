@model List<odev4.Models.Users>
@{
    ViewBag.Title = "Admin Sayfası";
}

<div class="panel panel-default">
    <div class="panel-heading">Kullanıcılar</div>
    <div class="panel-body">
        @if (Convert.ToBoolean(Session["loginRoles"]) == true)
        {
            <table class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th>No</th>
                        <th>Kullanıcı Adı</th>
                        <th>Sistem Giriş ismi</th>
                        <th>Parola</th>
                        <th>Tarih</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.id</td>
                            <td>@item.usernm</td>
                            <td>@item.loginnm</td>
                            <td>@item.pwd</td>
                            <td>@item.date</td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            Session["loginRoles"] = false;
            <div class="alert alert-danger" role="alert">
                <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
                <span class="sr-only">Hata:</span>
                Bu Sayfa İçin Yetkiniz Yok
            </div>
        }
    </div>
</div>